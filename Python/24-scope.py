# scope = the region that a variable is recognized. A variable is only available from inside the region it is created
#            A globla and locally scoped version of a variable can be created

# name = 'Bro' \\ variavel global disponivel dentro e fora da função

def display_name():
    name = 'César'  # \\ variavel local apenas disponivel dentro da funcção
    print(name)

