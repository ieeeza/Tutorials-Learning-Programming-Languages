# nested functions calls = function calls inside other function calls.
#                            innermost function calls are resolved first
#                            returned avalue is used as argument for the next other function


print(round(abs(float(input('Digite um numero inteiro positivo: ')))))